services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: jobfit
      POSTGRES_PASSWORD: jobfit
      POSTGRES_DB: jobfit
    ports:
      - "5432:5432"
    volumes:
      - jobfit-postgres:/var/lib/postgresql/data

volumes:
  jobfit-postgres:
